"""YOLOv8 military detector."""
import logging
logger = logging.getLogger(__name__)


def detect_objects(image_path: str) -> list:
    """Detect military objects using YOLOv8."""
    logger.info(f"Detecting objects in {image_path}")
    return []
